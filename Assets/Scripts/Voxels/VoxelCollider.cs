using UnityEngine;

// Just a dumb script to be able to get the global voxel position via the collider when colliding with a voxel
public class VoxelCollider : MonoBehaviour
{
    public Vector3Int GlobalVoxelPos { get; set; }

    void Start() {}

    void Update() {}
}
